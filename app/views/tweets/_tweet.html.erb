<div id="<%= tweet.id %>" class="card card-body mb-3">
  <div><%= tweet.user.email %></div>
  <div><%= tweet.body %></div>
  <div class="text-muted"><small><%= tweet.created_at %></small></div>
</div>
